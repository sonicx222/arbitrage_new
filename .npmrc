# NPM Security Configuration
# Prevents common supply chain attacks and enforces security best practices

# === Audit Settings ===
# Automatically audit packages on install (fail on moderate+ vulnerabilities)
audit-level=moderate

# === Lockfile Integrity ===
# Require package-lock.json to exist and be up-to-date
package-lock=true

# Prevent modifications to package-lock.json during install (CI safety)
# Uncomment for CI environments:
# package-lock-only=true

# === Version Pinning ===
# Save exact versions to prevent unintended updates
save-exact=true

# === Script Safety ===
# IMPORTANT: Scripts are needed for native modules (bcrypt, etc.)
# If you don't need native modules, uncomment to disable scripts:
# ignore-scripts=true

# === Registry Security ===
# Use HTTPS only (default, but explicit for security)
registry=https://registry.npmjs.org/

# Prevent accidental publishing to npm
# (Also handled by "private": true in package.json)
# Uncomment if you want extra protection:
# no-optional=true

# === Fund and Update Noise Reduction ===
# Disable funding messages during install
fund=false

# === Engine Strictness ===
# Fail if Node.js version doesn't match engines field
engine-strict=true

# === Progress and Logging ===
# Show progress bar during install
progress=true

# Useful for debugging dependency resolution:
# loglevel=verbose
