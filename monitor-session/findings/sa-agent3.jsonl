{"phase":"STATIC","findingId":"SA-3J-001","category":"UNSAFE_PARSE","severity":"INFO","title":"parseInt/parseFloat in services/shared production code all have NaN protection","service":"execution-engine, coordinator, shared/core","evidence":"services/execution-engine/src/engine.ts:363-415 uses parseInt/parseFloat with Number.isNaN guards. services/coordinator/src/api/middleware/index.ts:62-65 uses parseInt with Number.isNaN+>0 guards. shared/core/src/caching/hierarchical-cache.ts:55-56 uses parseInt with Number.isInteger guard. shared/core/src/logging/otel-transport.ts:134-140 uses parseInt with Number.isFinite guard. shared/core/src/async/event-batcher.ts:14-15 uses parseInt with Number.isNaN guard. shared/core/src/caching/gas-price-cache.ts:335-336 uses parseFloat with Number.isNaN guard. shared/config/src/thresholds.ts:33 uses parseFloat with Number.isNaN guard.","expected":"All parseInt/parseFloat(process.env.*) calls should have NaN protection","actual":"All 12 parseInt/parseFloat(process.env.*) calls in services/ and shared/ have NaN guards (Number.isNaN, Number.isFinite, Number.isInteger, or || fallback). Migration to parseEnvIntSafe/parseEnvFloatSafe is also in progress (base.strategy.ts, chain-instance.ts, auth.ts).","recommendation":"No action needed. Good pattern compliance."}
{"phase":"STATIC","findingId":"SA-3J-002","category":"UNSAFE_PARSE","severity":"LOW","title":"parseInt in contracts/ scripts lacks NaN guard","service":"contracts","evidence":"contracts/hardhat.config.ts:94 parseInt(process.env.FORK_BLOCK_NUMBER) — no radix parameter, no NaN guard. contracts/scripts/deploy-v3-adapter.ts:101 parseInt(process.env.DEFAULT_FEE_TIER ?? '3000', 10) — has ?? fallback but NaN if env var set to non-numeric.","expected":"All parseInt(process.env.*) should have NaN guard","actual":"Two parseInt calls in contracts/ lack explicit NaN protection. The hardhat.config one is guarded by the ternary (only called when FORK_BLOCK_NUMBER is truthy and !== 'latest'), but NaN would propagate silently if set to e.g. 'abc'. deploy-v3-adapter has ?? '3000' fallback but user-set non-numeric value produces NaN.","recommendation":"Add NaN guard: const raw = parseInt(...); if (Number.isNaN(raw)) throw new Error('Invalid FORK_BLOCK_NUMBER'). Low priority — these are dev/deploy scripts, not hot-path."}
{"phase":"STATIC","findingId":"SA-3J-003","category":"UNSAFE_PARSE","severity":"INFO","title":"parseEnvIntSafe/parseEnvFloatSafe adoption progressing","service":"execution-engine, unified-detector, security","evidence":"base.strategy.ts uses parseEnvIntSafe (6 calls) and parseEnvFloatSafe (1 call). chain-instance.ts uses parseEnvIntSafe (4 calls). auth.ts uses parseEnvIntSafe (1 call). engine.ts uses parseEnvIntSafe (3 calls).","expected":"Gradual migration to safe utilities","actual":"14 uses of parseEnvIntSafe/parseEnvFloatSafe across 4 production files. Older code in engine.ts still uses inline parseInt+NaN guard pattern. Both patterns are NaN-safe.","recommendation":"Continue migration to parseEnvIntSafe/parseEnvFloatSafe for consistency. Low priority — both patterns are functionally equivalent."}
{"phase":"STATIC","findingId":"SA-3K-001","category":"REDIS_CLIENT_PARITY","severity":"INFO","title":"RedisClient and RedisStreamsClient configuration fully aligned","service":"shared/core","evidence":"client.ts:218 maxRetriesPerRequest:3, lazyConnect:true, retryStrategy: capped exp backoff 200ms..60s (never null), connectTimeout:3000, enableReadyCheck:false, retryDelayOnFailover:100, reconnectOnError: READONLY. streams.ts:501 maxRetriesPerRequest:3, lazyConnect:true, retryStrategy: same capped exp backoff, connectTimeout:3000, enableReadyCheck:false (SA-1K-001 FIX), retryDelayOnFailover:100 (SA-1K-002 FIX), reconnectOnError: READONLY (SA-052 FIX).","expected":"All 4 dimensions aligned: retryStrategy, connectTimeout, maxRetriesPerRequest, lazyConnect","actual":"All 7 dimensions aligned: retryStrategy (same capped exponential), connectTimeout (3000ms), maxRetriesPerRequest (3), lazyConnect (true), enableReadyCheck (false), retryDelayOnFailover (100), reconnectOnError (READONLY). Previous SA-1K-001/002/052 fixes resolved all divergences.","recommendation":"No action needed. Excellent parity."}
{"phase":"STATIC","findingId":"SA-3M-001","category":"SILENT_ERROR","severity":"MEDIUM","title":"9 factory parsers silently return null on parse errors without logging","service":"shared/core","evidence":"algebra-parser.ts:53-55, balancer-v2-parser.ts:79-81 and 150-152, curve-parser.ts:92 and 160, solidly-parser.ts:58, trader-joe-parser.ts:55, v2-pair-parser.ts:52, v3-pool-parser.ts:61-63. All have catch(error) { return null; } with no logging.","expected":"Parse errors should be logged at debug/warn level for observability","actual":"9 catch blocks in factory-subscription parsers silently return null. These are event log parsers — a malformed log event would be silently dropped with no trace. While individually low impact (one missed pool), systematic silent failures could mask chain-specific issues (e.g., new contract versions with different log signatures).","recommendation":"Add this.logger.debug or console.warn in each catch block with the error and log.transactionHash for traceability. These are not hot-path (factory events are infrequent), so logging is safe."}
{"phase":"STATIC","findingId":"SA-3M-002","category":"SILENT_ERROR","severity":"LOW","title":"path-finding/multi-leg-path-finder.ts silently returns null on simulation error","service":"shared/core","evidence":"multi-leg-path-finder.ts:632-634 catch(error) { return null; } in simulateSwap(). No logging of the simulation failure.","expected":"Swap simulation errors should be logged for debugging","actual":"When simulateSwap fails (e.g., overflow, invalid pool state), it silently returns null. The caller skips this path. While functionally correct (failed simulation = skip path), persistent simulation failures for specific pools would be invisible.","recommendation":"Add debug-level logging: this.logger.debug('Swap simulation failed', { pool, error }). Low priority — callers handle null correctly."}
{"phase":"STATIC","findingId":"SA-3M-003","category":"SILENT_ERROR","severity":"INFO","title":"Health-check ping methods correctly return false without logging","service":"shared/core","evidence":"client.ts:1206 ping() catch returns false. streams.ts:1393 ping() catch returns false. client.ts:1447 checkRedisConnection() returns false.","expected":"Ping/healthcheck methods may silently return false","actual":"These are intentional — ping() is called frequently by health checks, and logging every transient Redis connectivity blip would be noisy. The caller (health endpoint) handles false by reporting unhealthy status.","recommendation":"No action needed. Correct pattern for health checks."}
{"phase":"STATIC","findingId":"SA-3M-004","category":"SILENT_ERROR","severity":"INFO","title":"All service-layer catch blocks log before returning","service":"coordinator, execution-engine, unified-detector","evidence":"coordinator/src/alerts/notifier.ts:116 logs error before return false. unified-detector/src/chain-instance.ts:2142 logs warn before return null. execution-engine/src/strategies/cross-chain.strategy.ts:194 logs debug before return undefined. All 7 catch-return patterns in services/ production code log before returning.","expected":"Service catch blocks should log errors","actual":"All service-layer catch blocks that return null/false/undefined also log the error at appropriate levels (error, warn, or debug). Good pattern.","recommendation":"No action needed."}
{"phase":"STATIC","findingId":"SA-3M-005","category":"SILENT_ERROR","severity":"INFO","title":"Total catch block audit: 887 catch blocks in production code, 0 truly empty","service":"all","evidence":"Grep for catch blocks in services/ and shared/ (excluding test files) found 887 catch blocks. Zero empty catch blocks (no catch {} pattern). 9 silent-return-without-log in shared/core parsers (SA-3M-001), 1 in path-finder (SA-3M-002). All others either log, rethrow, or are health-check pings.","expected":"No empty catch blocks","actual":"No truly empty catch blocks found. 10 catch blocks return without logging (9 parsers + 1 path finder). This is a significant improvement from earlier analysis where empty catches were flagged.","recommendation":"Address the 10 silent returns per SA-3M-001 and SA-3M-002."}
{"phase":"STATIC","findingId":"SA-3N-001","category":"STREAM_TYPE_FIDELITY","severity":"LOW","title":"Stream deserialization timestamp and expiresAt fields remain strings after validation","service":"execution-engine","evidence":"validation.ts:332-343 restores profitPercentage, confidence, expectedProfit, estimatedProfit via parseFloat. But timestamp (serialized as string on stream-serialization.ts:77) and expiresAt are NOT restored to numbers. Line 347: data as unknown as ArbitrageOpportunity cast means these string fields are typed as number.","expected":"All numeric fields should be restored to their TypeScript types after deserialization","actual":"4 of 6 numeric fields are properly restored (profitPercentage, confidence, expectedProfit, estimatedProfit). timestamp remains a string after cast. expiresAt is checked for expiry (converted to number locally) but not written back. JS minus-operator coerces strings to numbers, so Date.now() - timestamp works correctly in practice, but this is fragile type-safety.","recommendation":"Add restoration for timestamp: if (typeof data.timestamp === 'string') { data.timestamp = Number(data.timestamp); }. Also consider expiresAt. Low severity because JS coercion handles it, but explicit conversion would be safer."}
{"phase":"STATIC","findingId":"SA-3N-002","category":"STREAM_TYPE_FIDELITY","severity":"INFO","title":"Serialization uses isFinite guards to prevent NaN/Infinity poison messages","service":"coordinator","evidence":"stream-serialization.ts:72-88 guards profitPercentage, confidence, expectedProfit, estimatedProfit with (value != null && isFinite(value)) checks, defaulting to '0' if NaN/Infinity. SA-1N-003 FIX.","expected":"Numeric fields should be validated before serialization","actual":"All 4 key numeric fields have isFinite guards. This prevents NaN or Infinity values from propagating through Redis Streams as poison messages.","recommendation":"No action needed. Good defensive serialization."}
{"phase":"STATIC","findingId":"SA-3N-003","category":"STREAM_TYPE_FIDELITY","severity":"INFO","title":"getOptionalNumber type guard handles string-encoded numerics correctly","service":"coordinator","evidence":"type-guards.ts:61-68 getOptionalNumber() checks typeof === 'number' first, then typeof === 'string' with Number() conversion and NaN guard. SA-1N-001 FIX applied.","expected":"Type guards should handle both native numbers and string-encoded numerics from Redis","actual":"getOptionalNumber correctly handles both types. Previously only checked typeof === 'number' which would fail for all Redis-sourced values. Fix applied.","recommendation":"No action needed."}
{"phase":"STATIC","findingId":"SA-3N-004","category":"STREAM_TYPE_FIDELITY","severity":"MEDIUM","title":"as unknown as ArbitrageOpportunity unsafe cast bypasses type checking","service":"execution-engine","evidence":"validation.ts:347 'data as unknown as ArbitrageOpportunity'. After manual parseFloat restoration of 4 fields, the remaining Record<string, unknown> is cast wholesale. Fields like buyPrice (number), sellPrice (number), blockNumber (number), amount (number) from the ArbitrageOpportunity interface remain as strings but are typed as numbers.","expected":"Either restore all numeric fields or use a proper deserialization function","actual":"Only 4 of ~8 numeric fields are explicitly restored. buyPrice, sellPrice, blockNumber, and other numeric fields remain as strings. The cast masks this type mismatch from TypeScript. Downstream code using these fields may behave unexpectedly (e.g., buyPrice + sellPrice would concatenate strings instead of adding numbers).","recommendation":"Create a deserializeOpportunity() function that restores all numeric fields, or at minimum add parseFloat for buyPrice, sellPrice, blockNumber. MEDIUM because unreferenced fields may not cause runtime bugs today, but adding new downstream code that uses them would silently fail."}
{"phase":"STATIC","findingId":"SA-3O-001","category":"REDIS_KEY_REGISTRY","severity":"INFO","title":"Redis key prefix inventory — 18 documented prefixes with clear ownership","service":"all","evidence":"Key prefixes found in production code: (1) coordinator:leader:lock — coordinator leadership. (2) lock:execution: — execution engine distributed locks. (3) health:<serviceName> — per-service health (client.ts, self-healing-manager.ts). (4) health:system:snapshot — system-wide health (SA-1O-002 FIX). (5) metrics:<service>:<bucket> — performance metrics. (6) price:<chainId>:<pairAddr> — price cache (unified-detector). (7) pair:<chain>:<dex>:<t0>:<t1> — pair cache (pair-cache.ts). (8) bridge:recovery:<bridgeId> — cross-chain bridge recovery. (9) bridge:recovery:corrupt:<key> — corrupt bridge data quarantine. (10) commit-reveal:<chain>:<hash> — commit-reveal state. (11) ratelimit:<id> — rate limiting (default prefix). (12) api:<id> — API rate limiting. (13) arbitrage:<id> — arbitrage rate limiting. (14) auth:<type>:<id> — auth lockout/attempts/blacklist. (15) critical:<id> — critical ops rate limiting. (16) dlq:<id> — dead letter queue entries. (17) risk:probabilities: — risk probability tracker. (18) ab-test:<id> — A/B testing metrics. (19) opp:dedup:<id> — opportunity deduplication. (20) region:health: — cross-region health. (21) cache:l2: — hierarchical L2 cache. Stream names: 20 stream: prefixed names in RedisStreams constant.","expected":"Clear prefix registry with no collisions","actual":"21 key prefixes + 20 stream names. All use distinct namespaces with no collisions detected.","recommendation":"Consider documenting the key prefix registry in a dedicated file for operational reference."}
{"phase":"STATIC","findingId":"SA-3O-002","category":"REDIS_KEY_REGISTRY","severity":"LOW","title":"health: prefix used by both per-service health and system snapshot without sub-namespace","service":"shared/core","evidence":"client.ts:1035 uses health:<serviceName> (e.g., health:coordinator). enhanced-health-monitor.ts:544 uses health:system:snapshot (SA-1O-002 FIX). self-healing-manager.ts:734 uses health:<serviceName>.","expected":"health: prefix should have consistent sub-namespace hierarchy","actual":"health: prefix has two usages: (1) health:<serviceName> for per-service health (e.g., health:coordinator, health:execution-engine), and (2) health:system:snapshot for system-wide snapshot. SA-1O-002 already fixed the previous collision by adding 'system:' sub-namespace. Current state is safe because 'system' is not a valid service name. However, if a service named 'system' were added, it would collide.","recommendation":"No immediate action needed. The SA-1O-002 fix resolved the actual collision. Document the 'system' reserved name to prevent future collision."}
{"phase":"STATIC","findingId":"SA-3O-003","category":"REDIS_KEY_REGISTRY","severity":"LOW","title":"Rate limiter prefixes (ratelimit, api, arbitrage, auth, critical) are not sub-namespaced under a common parent","service":"shared/security","evidence":"rate-limiter.ts creates 4 factory instances with different keyPrefix values: 'api', 'arbitrage', 'auth', 'critical'. Default prefix is 'ratelimit'. These are top-level Redis key namespaces without a common parent prefix.","expected":"Rate limiting keys should be namespaced under a common prefix like 'rl:api:', 'rl:auth:'","actual":"5 different top-level prefixes for rate limiting. 'api:' and 'auth:' are short, generic prefixes that could theoretically collide with other subsystems. Currently no collision because auth.ts uses 'auth:blacklist:', 'auth:lockout:', 'auth:attempts:' while rate-limiter uses 'auth:<identifier>' (hashed identifier). The keys don't collide in practice because auth.ts sub-keys have 3 segments (auth:blacklist:token) while rate-limiter uses 2 (auth:hashedId).","recommendation":"Consider renaming rate-limiter prefixes to 'rl:api:', 'rl:auth:', etc. to avoid future collision risk. Low priority — no actual collision exists today."}
